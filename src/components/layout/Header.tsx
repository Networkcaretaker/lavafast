// Header.tsx
import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="relative z-10 py-4">
      {/* You can add header content here if needed, or leave empty */}
      {/* This could be used for a hero section, breadcrumbs, or page-specific headers */}
    </header>
  );
};

export default Header;